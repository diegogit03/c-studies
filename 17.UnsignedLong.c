void main()
    {
        /*

            Com a mistura de ambos os modificadores de tipo(unsigned e long), podemos
            fazer a seguinte operação:

            dobrar a quantia de bits e tirar um bit de sinal

        */

        unsigned long int valor_muito_grande;
        unsigned long int divida_publica;
    }
