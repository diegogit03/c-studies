#include <stdio.h>

void main(void)
    {
        /*

            A variavel do tipo char, ocupa ao total 8 bits, ou 1 byte tendo 1 bit somente para o sinal,
            variaveis do tipo char também podem carregar numeros, entre -128 a 127, esse numero deve representar
            o codigo decimal ASCII da letra.

            tabela com os valores decimais ASCII: https://pt.wikipedia.org/wiki/ASCII

        */

        char primeira_letra_do_alfabeto_ascii = 65; // aqui será o mesmo resultado, da variavel abaixo
        char primeira_letra_do_alfabeto = 'a'; // representação de "a" em bytes
    }
