#include <stdio.h>

void Main(void)
    {
        // o programa não passa pela compilação, poís o main esta maisculo.
        printf("Este programa não passa pela compilação.");

        // mais sobre o main:
        // o main é o primeiro "bloco" de codigo a ser executado, ou seja é por onde o compilador começa, e o erro acima acontece,
        // poís o compilador conhece somente "main" e não "Main".
    }
