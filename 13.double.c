#include <stdio.h>

void main(void)
    {
        /*

            o tipo double ocupa 64bits(8 bytes), ela aceita numeros decimais, e aguenta mais
            digitos que o tipo float, de 14 a 15 são os limites.

        */
        float raio;
        raio = 123.45; // mesmo resultado abaixo
        raio = 1.2345E2; // Formato expodencials
    }
